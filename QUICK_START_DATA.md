# Быстрый старт: подготовка фото для обучения

## 📁 Куда положить фото

**Создайте папку с именем персоны в `data/input_photos/`**

Например:
```
data/input_photos/
└── alex/          ← Создайте эту папку
    ├── photo1.jpg
    ├── photo2.jpg
    ├── photo3.png
    └── ...
```

## 📸 Требования к фото

- **Количество**: 40-80 фото (можно меньше, но качество может быть хуже)
- **Форматы**: JPG, PNG, WEBP
- **Разнообразие**:
  - Фронтальные кадры
  - Профиль (3/4, боковой)
  - Разные эмоции (улыбка, серьёзное выражение)
  - Разное освещение
  - 2-3 фото по пояс и в полный рост
- **Качество**: 
  - Без тяжёлых фильтров
  - Без сильной ретуши
  - Хорошее разрешение (минимум 512x512)

## 🚀 Запуск обработки

После того как положите фото в папку, запустите:

```bash
python scripts/auto_prepare_data.py --person alex --gender male --age 30 --hair_color brown
```

Или просто:
```bash
python scripts/auto_prepare_data.py --person alex
```

Скрипт автоматически:
1. ✅ Найдёт все фото в папке
2. ✅ Обработает их (изменение размера, очистка EXIF)
3. ✅ Создаст подписи с триггер-токеном
4. ✅ Подготовит датасет для обучения

## 📋 Параметры

- `--person` - имя персоны (обязательно)
- `--gender` - пол (male/female, опционально)
- `--age` - возраст (опционально)
- `--hair_color` - цвет волос (опционально)

## ✅ Что получится

После обработки будет создана структура:
```
data/datasets/alex/
├── images/              # Обработанные изображения
├── metadata.jsonl      # Подписи с триггер-токенами
├── manifest.json       # Манифест персоны
└── tokenizer/          # Токенайзер с добавленным токеном
```

## 🎯 Следующий шаг

После подготовки данных:
```bash
python scripts/train_lora.py --person alex
```

