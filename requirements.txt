# Core training/inference stack
torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.25.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.7.0
safetensors>=0.4.2

# Data preparation & preprocessing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0
tqdm>=4.66.0
pyyaml>=6.0

# Optional extras (install manually if needed)
# insightface>=0.7.3        # Face-ID quality checks
# onnxruntime-gpu>=1.16.0   # GPU backend for InsightFace
# pynvml>=11.5.0            # VRAM diagnostics in scripts/inference.py

