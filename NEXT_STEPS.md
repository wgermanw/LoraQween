# Следующие шаги

## ✅ Что уже готово

Проект LoraQween полностью настроен и готов к использованию:

1. ✅ Структура проекта создана
2. ✅ Все зависимости установлены
3. ✅ Модули для работы с токенами, данными, обучением и инференсом готовы
4. ✅ Скрипты для подготовки данных, обучения и генерации созданы
5. ✅ Система контроля качества реализована
6. ✅ Управление VRAM настроено
7. ✅ Базовая интеграция с ComfyUI подготовлена

## 🎯 Что нужно сделать дальше

### 1. Скачать модель Qwen-Image 20B

**ВАЖНО**: Модель должна быть скачана отдельно из официального репозитория Qwen.

```bash
# Скачать токенайзер и процессор (уже работает)
python scripts/download_base_model.py

# Затем скачать саму модель Qwen-Image 20B в models/base/
# См. docs/MODEL_DOWNLOAD.md для подробностей
```

### 2. Подготовить данные для обучения

Соберите 40-80 фотографий персоны и запустите:

```bash
python scripts/prepare_data.py \
    --person alex \
    --input_dir /path/to/photos \
    --gender male \
    --age 30 \
    --hair_color brown
```

### 3. Обучить LoRA

После скачивания модели:

```bash
python scripts/train_lora.py --person alex
```

⚠️ **Примечание**: Код обучения требует доработки после получения модели Qwen-Image 20B.

### 4. Запустить генерацию

```bash
# Быстрый режим
python scripts/inference.py \
    --person alex \
    --prompt "<qwn_alex> portrait, smiling" \
    --mode fast \
    --num_images 4

# Надёжный режим
python scripts/inference.py \
    --person alex \
    --prompt "<qwn_alex> portrait, professional" \
    --mode reliable \
    --num_images 2 \
    --reference /path/to/reference.jpg
```

### 5. Проверить качество

```bash
python scripts/quality_control.py \
    --person alex \
    --generated_dir outputs/alex/generation_YYYYMMDD_HHMMSS \
    --reference_dir data/references/alex \
    --output reports/quality_report.json
```

## 📚 Документация

- `README.md` - Общая информация о проекте
- `SETUP_GUIDE.md` - Руководство по настройке
- `PROJECT_STATUS.md` - Детальный статус проекта
- `docs/INSTALLATION.md` - Подробная инструкция по установке
- `docs/QUICKSTART.md` - Быстрый старт
- `docs/MODEL_DOWNLOAD.md` - Инструкция по скачиванию модели

## ⚠️ Важные замечания

1. **Модель Qwen-Image 20B**: Критически важно скачать модель перед обучением и генерацией
2. **CUDA**: Убедитесь, что CUDA 12.x установлена и работает
3. **VRAM**: Проект оптимизирован для RTX 5060 Ti 16 GB, но может работать на других картах
4. **Токены**: Каждая персона должна иметь уникальный триггер-токен (создаётся автоматически)

## 🐛 Решение проблем

Если возникают проблемы:

1. Проверьте установку: `python scripts/setup.py`
2. Проверьте CUDA: `python -c "import torch; print(torch.cuda.is_available())"`
3. Проверьте конфигурацию: `python -c "from src.config import get_config; print('OK')"`

## 📞 Поддержка

Все модули готовы к использованию. После получения модели Qwen-Image 20B необходимо:

1. Обновить код загрузки модели в `src/training/lora_trainer.py`
2. Обновить код генерации в `src/inference/inference_engine.py`
3. Протестировать весь пайплайн

Удачи! 🚀

